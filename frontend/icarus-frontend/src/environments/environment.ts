// Local development environment
// API calls are relative paths proxied by the Angular dev-server (see proxy.conf.json)
export const environment = {
  production: false,
  apiAuth: '/api/auth',
  apiAdmin: '/api/admin',
  apiWorkflow: '/api/workflow',
};
